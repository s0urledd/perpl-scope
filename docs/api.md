# API status

No HTTP API is implemented. The validation CLI emits a JSON report containing
status, coverage, and available preflight evidence. It never serves fixtures as
production data. Future analytics endpoints must share an immutable snapshot ID.
